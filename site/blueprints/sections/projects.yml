headline: Projects
type: pages
layout: cards
parent: site.find("projects")
status: listed
templates: project
item:
  image:
    src: page.images.filterBy("template", "cover").first
    ratio: 3/2
  info: "{{ page.year }}"
