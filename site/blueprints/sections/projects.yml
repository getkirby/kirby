headline: Projects
type: pages
layout: cards
parent: site.find("projects")
status: listed
templates: project
item:
  image:
    src: page.images.template("cover").first
    ratio: 1/1
    cover: true
  info: "{{ page.year }}"
