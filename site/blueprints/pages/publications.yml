title: Publications

icon: 📖

columns:
  - 1/3: progress
  - 1/3: review
  - 1/3: approved
  - 1/1: published

sections:
  progress:
    headline: Work in progress
    type: pages
    query: page.drafts.filterBy("stage", "wip")
    layout: list
    create: publication
  review:
    headline: In review
    type: pages
    query: page.drafts.filterBy("stage", "review")
    layout: list
  approved:
    headline: Approved
    type: pages
    query: page.drafts.filterBy("stage", "approved")
    layout: list
  published:
    headline: Published
    type: pages
    query: page.children
    layout: list
