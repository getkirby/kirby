title: Events

sections:

  drafts:
    type: pages
    headline: Unpublished events
    query: page.drafts.sortBy("from", "asc")
    create: event
    item:
      info: "{{ page.from.toDate('d.m.Y') }} - {{ page.to.toDate('d.m.Y') }} / {{ page.location }}"

  published:
    type: pages
    headline: Published events
    query: page.children.sortBy("from", "asc")
    create: event
    item:
      info: "{{ page.from.toDate('d.m.Y') }} - {{ page.to.toDate('d.m.Y') }} / {{ page.location }}"
