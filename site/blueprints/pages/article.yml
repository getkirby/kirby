title: Article

num: date

icon: 💩

columns:
  - 2/3: content
  - 1/3: meta, images

sections:

  content:
    type: fields
    fields:
      text:
        label: Text
        type: textarea
        size: large

  meta:
    type: fields
    fields:
      date:
        label: Date
        type: datetime
        default: now
      tags:
        label: Tags
        type: tags
        options: query
        query: site.find("blog").children

  images:
    extends: sections/images
    template: image
