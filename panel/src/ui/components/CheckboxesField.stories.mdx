import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import Padding from "../storybook/Padding.js";
import Options from "../storybook/Options.js";
import CheckboxesField from "./CheckboxesField.vue";

<Meta
  title="UI | Form / Field / Checkboxes Field"
  component={CheckboxesField}
  decorators={[Padding]}
/>

# Checkboxes Field
### `<k-checkboxes-field>`

<Preview>
  <Story name="Regular">
    {{
      data() {
        return {
          value: [],
        };
      },
      computed: {
        options() {
          return Options();
        }
      },
      methods: {
        input: action("input")
      },
      template: `
        <div>
          <k-checkboxes-field
            v-model="value"
            :options="options"
            class="mb-8"
            label="Checkboxes"
            @input="input"
          />
          <k-headline class="mb-3">Value</k-headline>
          <k-code-block :code="value" />
        </div>
      `,
    }}
  </Story>
</Preview>

<Props of={CheckboxesField} />

#### Methods

**`focus()`**
Set the focus on the field.

#### Events

**`@input`**
The input event is triggered when the value changes. The new value is passed.


## More examples

### Disabled
<Preview>
  <Story name="Disabled">
    {{
      data() {
        return {
          value: [],
        };
      },
      computed: {
        options() {
          return Options();
        }
      },
      methods: {
        input: action("input")
      },
      template: `
        <k-checkboxes-field
          v-model="value"
          :disabled="true"
          :options="options"
          label="Checkboxes"
          @input="input"
        />
      `,
    }}
  </Story>
</Preview>

### Autofocus
<Preview>
  <Story name="Autofocus">
    {{
      data() {
        return {
          value: [],
        };
      },
      computed: {
        options() {
          return Options();
        }
      },
      methods: {
        input: action("input")
      },
      template: `
        <k-checkboxes-field
          v-model="value"
          :autofocus="true"
          :options="options"
          label="Checkboxes"
          @input="input"
        />
      `,
    }}
  </Story>
</Preview>

### Columns
<Preview>
  <Story name="Columns">
    {{
      data() {
        return {
          value: [],
        };
      },
      computed: {
        options() {
          return Options();
        }
      },
      methods: {
        input: action("input")
      },
      template: `
        <k-checkboxes-field
          v-model="value"
          :columns="3"
          :options="options"
          label="Checkboxes"
          @input="input"
        />
      `,
    }}
  </Story>
</Preview>

### Checkboxes vs. Radios
<Preview>
  <Story name="Checkoxes vs. Radios">
    {{
      data() {
        return {
          checkboxes: [],
          radios: [],
        };
      },
      computed: {
        checkboxOptions() {
          return Options();
        },
        radioOptions() {
          return Options();
        }
      },
      methods: {
        input: action("input")
      },
      template: `
        <k-grid gutter="large">
          <k-column width="1/2">
            <k-checkboxes-field
              v-model="checkboxes"
              :columns="3"
              :options="checkboxOptions"
              class="mb-6"
              label="Checkboxes"
              @input="input"
            />
            <k-code-block :code="checkboxes" />
          </k-column>
          <k-column width="1/2">
            <k-radio-field
              v-model="radios"
              :columns="3"
              :options="radioOptions"
              class="mb-6"
              label="Radios"
              @input="input"
            />
            <k-code-block :code="radios" />
          </k-column>
        </k-grid>
      `,
    }}
  </Story>
</Preview>

## CSS class
`.k-checkboxes-field`
