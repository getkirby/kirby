import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import Padding from "../storybook/Padding.js";
import HeaderBar from "./HeaderBar.vue";

<Meta
  title="UI | Layout / Header Bar"
  component={HeaderBar}
  decorators={[Padding]}
/>

# Header Bar
### `<k-header-bar>`

```html
<k-header-bar>Pages</k-header-bar>
```

<Props of={HeaderBar} />

### Simple
<Preview>
  <Story name="Simple">
    {{
      template: `
        <k-header-bar text="Pages" />
      `,
    }}
  </Story>
</Preview>

### Single Option
<Preview>
  <Story name="Single Option">
    {{
      computed: {
        options() {
          return [
            { icon: "add", text: "Add", option: "add" },
          ];
        }
      },
      template: `
        <k-header-bar
          :options="options"
          text="Pages"
        />
      `,
    }}
  </Story>
</Preview>

### Multiple Options
<Preview>
  <Story name="Multiple Options">
    {{
      computed: {
        options() {
          return [
            { icon: "upload", text: "Upload", option: "upload" },
            { icon: "check", text: "Select", option: "select" },
          ];
        }
      },
      template: `
        <k-header-bar
          :options="options"
          text="Pages"
        />
      `,
    }}
  </Story>
</Preview>

### Options icon & text
<Preview>
  <Story name="Options icon & text">
    {{
      computed: {
        options() {
          return [
            { icon: "upload", text: "Upload", option: "upload" },
            { icon: "check", text: "Select", option: "select" },
          ];
        }
      },
      template: `
        <k-header-bar
          :options="options"
          options-icon="add"
          options-text="Add"
          text="Pages"
        />
      `,
    }}
  </Story>
</Preview>

### Link & Options
<Preview>
  <Story name="Link & Options">
    {{
      computed: {
        options() {
          return [
            { icon: "upload", text: "Upload", option: "upload" },
            { icon: "check", text: "Select", option: "select" },
          ];
        }
      },
      template: `
        <k-header-bar
          :options="options"
          link="https://getkirby.com"
          options-icon="add"
          options-text="Add"
          text="Pages"
        />
      `,
    }}
  </Story>
</Preview>

### Required
<Preview>
  <Story name="Required">
    {{
      computed: {
        options() {
          return [
            { icon: "upload", text: "Upload", option: "upload" },
            { icon: "check", text: "Select", option: "select" },
          ];
        }
      },
      template: `
        <k-header-bar
          :options="options"
          :required="true"
          link="https://getkirby.com"
          options-icon="add"
          options-text="Add"
          text="Pages"
        />
      `,
    }}
  </Story>
</Preview>

### Label for
<Preview>
  <Story name="Label for">
    {{
      computed: {
        options() {
          return [
            { icon: "upload", text: "Upload", option: "upload" },
            { icon: "check", text: "Select", option: "select" },
          ];
        }
      },
      template: `
        <k-header-bar
          :options="options"
          :required="true"
          element="label"
          for="my-input"
          options-icon="add"
          options-text="Add"
          text="Pages"
        />
      `,
    }}
  </Story>
</Preview>

## CSS class

`.k-header-bar`
