import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import Padding from "../storybook/Padding.js";
import { Users } from "../storybook/PickerItems.js";
import UsersPicker from "./UsersPicker.vue";

<Meta
  title="UI | Data / Picker / Users Picker"
  component={UsersPicker}
  decorators={[Padding]}
/>

# Pages Picker
### `<k-pages-picker>`

<Preview>
  <Story name="Regular">
    {{
      extends: Users(),
      template: `
        <div>
          <k-users-picker
            v-model="value"
            :options="options"
            :pagination="pagination"
            @paginate="onPaginate"
            layout="cardlets"
            label="Pick your favorite"
          />
          <k-headline class="mt-8 mb-3">Value</k-headline>
          <k-code-block :code="value" />
        </div>
      `
    }}
  </Story>
</Preview>

<Props of={UsersPicker} />

## CSS class

`.k-users-picker`
