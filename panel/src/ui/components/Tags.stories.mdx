import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import Padding from "../storybook/Padding.js";
import Tags from "./Tags.vue";

<Meta
  title="UI | Interaction / Tags"
  component={Tags}
  decorators={[Padding]}
/>

# Tags
### `<k-tags>`

<Preview>
  <Story name="Regular">
    {{
      computed: {
        tags() {
          return [
            { text: "Design", value: "design" },
            { text: "Photography", value: "photography" }
          ];
        }
      },
      template: '<k-tags :value="tags" />',
    }}
  </Story>
</Preview>

<Props of={Tags} />


### List layout
<Preview>
  <Story name="List layout">
    {{
      computed: {
        tags() {
          return [
            { text: "Design", value: "design" },
            { text: "Photography", value: "photography" }
          ];
        }
      },
      template: '<k-tags :value="tags" layout="list" />',
    }}
  </Story>
</Preview>

### Non-removable
<Preview>
  <Story name="Non-removable">
    {{
      computed: {
        tags() {
          return [
            { text: "Design", value: "design" },
            { text: "Photography", value: "photography" }
          ];
        }
      },
      template: '<k-tags :value="tags" :removable="false" />',
    }}
  </Story>
</Preview>

## CSS class

`.k-tags`
