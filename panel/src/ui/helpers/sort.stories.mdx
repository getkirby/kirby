import { Meta } from '@storybook/addon-docs/blocks';

<Meta
  title="Internals | $helper / sort"
/>

# sort
### `this.$helper.sort`

Natural sort algorithm with unicode support

#### Example

```js
return this.$helper.sort(valueA, valueB);
```
