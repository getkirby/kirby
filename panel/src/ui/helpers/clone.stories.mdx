import { Meta } from '@storybook/addon-docs/blocks';

<Meta
  title="Internals | $helper / clone"
/>

# clone
### `this.$helper.clone`

Shorthand helper for

```js
JSON.parse(JSON.stringify(array))
```

#### Example
```js
let array = ["homer, "simpson"];
let cloned = this.$helper.clone(array);
```
