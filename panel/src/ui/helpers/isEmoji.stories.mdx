import { Meta } from '@storybook/addon-docs/blocks';

<Meta
  title="Internals | $helper / isEmoji"
/>

# isEmoji
### `this.$helper.isEmoji`

Returns a boolean whether the passed string is an emoji.

#### Example
```js
this.$helper.isEmoji(🙌)
// true
```
