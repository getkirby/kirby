<style>
:root {
	--kbd-color-back: var(--color-gray-300);
}

kbd {
	display: inline-flex;
	padding-inline: var(--spacing-1);
	background: var(--kbd-color-back);
	border-radius: var(--rounded);
	font-family: inherit;
}
</style>
